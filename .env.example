# Configurações de Banco de Dados
# Formato: HOST;USUARIO;SENHA;BANCO1,BANCO2,...
# Separe múltiplos servidores com ponto e vírgula (;)
DATABASES_CONFIG=localhost;mainuser;mainpass;maindb1,maindb2;
                192.168.1.100;seconduser;secondpass;seconddb1,seconddb2;
                remote.server.com;remoteuser;remotepass;remotedb1,remotedb2

# Configurações de Backup
# Número máximo de backups em paralelo
MAX_PARALLEL_BACKUPS=3

# Quantidade máxima de arquivos de backup a serem mantidos
MAX_BACKUP_FILES=7

# Modo de backup: 'full' (completo) ou 'incremental'
BACKUP_MODE=full

# Número máximo de tentativas de backup em caso de falha
MAX_RETRY_ATTEMPTS=3

# Intervalo de espera (em segundos) entre as tentativas de backup
RETRY_DELAY=30

# Diretório onde os backups serão armazenados
# Use um caminho absoluto
BACKUP_DIRECTORY=/home/usuario/database_backups/

# Configuração do Discord para Notificações
# URL do Webhook do Discord para receber notificações
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/seu/webhook/url
